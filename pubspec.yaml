name: videogame_rating
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.7.2


dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

  cupertino_icons: ^1.0.8
  flutter_launcher_icons: ^0.14.4
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2
  path: ^1.9.1
  change_app_package_name: ^1.5.0
  url_launcher: ^6.3.1
  flutter_rating_bar: ^4.0.1
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  http: ^1.4.0
  translator: ^1.0.3+1
  speech_to_text: ^7.0.0
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0


flutter:


  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/perfil.jpg
    - assets/profile.png
    - assets/Logo.png
    - lib/data/models/questions.json
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
flutter_launcher_icons:
 android: "ic_launcher" # Generar iconos para Android o reemplaza el vigente
 ios: false # Solo para Android
 image_path: "assets/Logo.png" # Ruta a tu archivo de icono
 min_sdk_android: 21 # Mínimo SDK de Android soportado
